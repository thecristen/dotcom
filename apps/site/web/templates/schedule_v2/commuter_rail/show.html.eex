<%= Site.ScheduleV2View.render "_route.html", forward_assigns(@conn) %>
<%= Site.ScheduleV2View.render "_direction.html", forward_assigns(@conn) %>
<%= tab_list(%TabList{
  links: [
    {"Time Table", Site.ScheduleV2View.update_schedule_url(@conn, tab: "timetable"), tab_selected?("timetable", @tab)},
    {"Trip View", Site.ScheduleV2View.update_schedule_url(@conn, tab: "trip-view"), tab_selected?("trip-view", @tab)}
  ]
}) %>
<%= render template_for_tab(@tab), forward_assigns(@conn) %>
