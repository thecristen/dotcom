<%= for {mode, routes} <- @grouped_routes do %>
  <%= render schedule_template(mode), Map.merge(forward_assigns(@conn), %{mode: mode, routes: routes}) %>
<% end %>
<span aria-hidden="true">
  <%= fa "rss station-schedule-icon" %> Real time service
</span>
