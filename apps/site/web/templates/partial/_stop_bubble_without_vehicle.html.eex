<svg viewBox="0 0 42 42" class="<%= String.trim(~s(icon stop-bubble-#{@class} #{@icon_class})) %>">
  <circle r="20" cx="20" cy="20" transform="<%= assigns[:transform] || "translate(2,2)" %>"></circle>
  <%= @route_id %>
  <text font-size="24" x="14" y="30">
    <%= @content %>
  </text>
</svg>
