defmodule Site.StyleGuideControllerTest do
  use Site.ConnCase

  # test "lists all entries on index", %{conn: conn} do
  #   conn = get conn, style_guide_path(conn, :index)
  #   assert html_response(conn, 200) =~ "Listing styleguides"
  # end

end
