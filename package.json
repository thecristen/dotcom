{
  "scripts": {
    "install": "cd apps/site && npm install",
    "test": "mix test && npm run test:js && mix backstop.tests",
    "backstop:test": "cd apps/site/node_modules/backstopjs && npm run test",
    "backstop:reference": "cd apps/site/node_modules/backstopjs && npm run reference",
    "backstop:bless": "cd apps/site/node_modules/backstopjs && npm run bless",
    "test:js": "cd apps/site && npm run test",
    "brunch:build": "cd apps/site && node_modules/brunch/bin/brunch b -p",
    "server:mocked": "MIX_ENV=prod PORT=8082 STATIC_SCHEME=http STATIC_HOST=localhost STATIC_PORT=8082 V3_URL=http://localhost:8080 mix do clean, deps.compile, compile, phoenix.server",
    "wiremock": "cd apps/site && java -jar $WIREMOCK_PATH",
    "wiremock:record": "cd apps/site && rm -r __files mappings && java -jar $WIREMOCK_PATH --proxy-all=\"http://mbta-api-dev.us-east-1.elasticbeanstalk.com\" --record-mappings",
    "routes": "cd apps/site && mix phoenix.routes && cd ../..",
    "dialyzer": "mix dialyzer | grep -E -v \"(variable _ can never match)|(Stops[.]Position.*__impl__)\""
  },
  "dependencies": {
    "brunch": "^2.8.2"
  }
}
